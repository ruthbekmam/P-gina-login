<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Mundo do Aprendizado</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Estilos Globais */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #ff6b6b 0%, #ff47da 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* Container Principal */
        .login-container {
            background: rgba(255, 107, 107, 0.15);
            padding: 40px 30px;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(255, 71, 218, 0.3);
            width: 100%;
            max-width: 400px;
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        /* Cabeçalho */
        .welcome-message {
            color: white;
            font-size: 28px;
            margin-bottom: 35px;
            text-align: center;
            line-height: 1.4;
            text-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        /* Formulário */
        .form-group {
            position: relative;
            margin-bottom: 30px;
        }

        .form-group input {
            width: 100%;
            padding: 15px 45px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 12px;
            background: rgba(255, 107, 107, 0.2);
            color: white;
            font-size: 16px;
            transition: 0.3s;
        }

        .form-group i {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: rgba(255, 255, 255, 0.8);
            transition: 0.3s;
        }

        .form-group input:focus {
            border-color: #ff47da;
            background: rgba(255, 107, 107, 0.3);
        }

        /* Botão de Login */
        .login-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(45deg, #ff6b6b, #ff47da);
            border: none;
            border-radius: 12px;
            color: white;
            font-size: 18px;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 15px;
            box-shadow: 0 5px 15px rgba(255, 71, 218, 0.4);
        }

        .login-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(255, 71, 218, 0.5);
        }

        /* Links Adicionais */
        .footer-links {
            text-align: center;
            margin-top: 25px;
        }

        .footer-links a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
            font-weight: 500;
            transition: 0.3s;
        }

        .footer-links a:hover {
            color: #ff47da;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="welcome-message">
            <i class="fas fa-gamepad" style="font-size: 56px; color: white; margin-bottom: 20px;"></i><br>
            Que bom te ver novamente!<br>
            Vamos continuar explorando o mundo do conhecimento?
        </div>
        
        <form>
            <div class="form-group">
                <i class="fas fa-user"></i>
                <input type="text" placeholder="Seu nome de usuário" required>
            </div>
            
            <div class="form-group">
                <i class="fas fa-lock"></i>
                <input type="password" placeholder="Sua senha secreta" required>
            </div>
            
            <button type="submit" class="login-btn">Entrar no Universo do Aprendizado</button>
        </form>

        <div class="footer-links">
            <a href="#">Recuperar senha</a>
            <a href="#">Criar nova conta</a>
        </div>
    </div>
</body>
</html>
